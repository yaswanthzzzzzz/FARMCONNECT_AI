import PhasePlaceholder from "@/pages/PhasePlaceholder";

export default function Aggregation() {
  return <PhasePlaceholder eyebrow="Aggregation opportunities" title="Make larger demand reachable for smaller farms." description="Aggregation will bring compatible nearby supply together when one farm alone cannot fulfil a buyer requirement." phase="Phase 3 · Farmer aggregation" detail="The foundation already includes farmer and buyer supply models. The next phase will add transparent grouping, quantity arithmetic and the logistics trade-offs behind a combined fulfilment path." backHref="/" />;
}
